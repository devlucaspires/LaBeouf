import 'package:flutter/material.dart';

class MontarTreinoScreen extends StatelessWidget {
  const MontarTreinoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Montar Treino')),
      body: Center(child: Text('Em breve...')),
    );
  }
}